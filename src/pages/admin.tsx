// DEMO: /admin — painel de operação (KPIs + pipeline de diagnósticos) (F4). Scaffold.
const AdminPage = () => {
  return (
    <div className="grid min-h-screen place-items-center text-muted-foreground">
      Painel — em construção
    </div>
  )
}

export default AdminPage
