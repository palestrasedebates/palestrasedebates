// DEMO: /plano/:id — leitura geral + 12 cards com gate de e-mail (F3). Scaffold.
const PlanoPage = () => {
  return (
    <div className="grid min-h-screen place-items-center text-muted-foreground">
      Plano — em construção
    </div>
  )
}

export default PlanoPage
