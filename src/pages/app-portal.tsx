// DEMO: /app — portal do cliente (magic link, plano | consultor IA) (F4). Scaffold.
const AppPortalPage = () => {
  return (
    <div className="grid min-h-screen place-items-center text-muted-foreground">
      Portal — em construção
    </div>
  )
}

export default AppPortalPage
